---
# Sentinel Configuration

# Staff mode tool cooldowns (seconds)
vanish-cooldown: 1.0
freeze-cooldown: 0.5

# History query limits (rows returned per lookup)
command-history-limit: 10
block-history-limit: 10
container-history-limit: 10
...
