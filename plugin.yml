name: Sentinel
version: 1.1.0
main: Phoenix4041\Sentinel\Loader
api: ["5.0.0"]
author: Phoenix4041
description: Unified staff toolkit for PocketMine-MP 5.x - one staff mode with a deduplicated NBT-tagged tool kit (teleport, freeze, vanish, invsee, ender inventory, history, ban, night vision), SQLite-backed command/block/container/kill audit logging, and sanctions/bans.

permissions:
  sentinel.staff:
    description: Access to /staff, staff mode and its tool kit
    default: op
  sentinel.command.unban:
    description: Access to /unban
    default: op
